const form=document.getElementById('registration-form'),quantity=document.getElementById('quantity'),total=document.getElementById('total'),guestWrap=document.getElementById('guest-names-wrap'),guestNames=document.getElementById('guestNames'),message=document.getElementById('form-message'),submitButton=document.getElementById('submit-button');function updateTotal(){const c=Number(quantity.value);total.textContent=`$${c*67}`;const g=c>1;guestWrap.classList.toggle('hidden',!g);guestNames.required=g;if(!g)guestNames.value=''}quantity.addEventListener('change',updateTotal);updateTotal();form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='';submitButton.disabled=true;submitButton.textContent='Preparing checkout…';const fd=new FormData(form),p=Object.fromEntries(fd.entries());p.quantity=Number(p.quantity);p.picnic=fd.get('picnic')==='Yes';p.concert=fd.get('concert')==='Yes';try{const r=await fetch('/api/create-checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)}),d=await r.json();if(!r.ok||!d.url)throw new Error(d.error||'Unable to create checkout.');location.href=d.url}catch(err){message.textContent=`${err.message} Please try again or email sharlaruiz@gmail.com.`;submitButton.disabled=false;submitButton.textContent='Continue to secure payment'}});
